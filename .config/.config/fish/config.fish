if type -q nvim
    alias vim "nvim"
end
if type -q rg
    alias grep "rg"
end
if type -q bat
    alias cat "bat"
end

alias c "clear"
alias ls 'ls --color=auto'
alias ll 'ls -alF'
alias la 'ls -A'
alias grep "grep --color=auto"
alias diff "diff --color=auto"
