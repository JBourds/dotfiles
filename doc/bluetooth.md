# Bluetooth

```shell
# Install the necessary packages, as well as a slick TUI
sudo pacman -S bluez bluez-utils blueman bluetui

# Enable bluetooth in systemd
sudo systemctl enable blueooth

# Connect using bluetui
bluetui
```
